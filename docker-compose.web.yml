services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "4200:80"
    depends_on:
      - api
    networks:
      - formunauts

networks:
  formunauts:
    external: true
